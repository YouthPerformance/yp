# ═══════════════════════════════════════════════════════════
# YP SHOP - SHOPIFY HYDROGEN STOREFRONT
# shop.youthperformance.com
# ═══════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ─────────────────────────────────────────────────────────────
# REQUIRED: Shopify Store Configuration
# ─────────────────────────────────────────────────────────────

# Your Shopify store domain (e.g., mystore.myshopify.com)
PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# Storefront API version (check compatibility with @shopify/hydrogen)
PUBLIC_STOREFRONT_API_VERSION=2024-10

# Storefront API Token (Public - for client-side queries)
# Create at: Shopify Admin > Settings > Apps > Develop apps > [Your App] > API credentials
# Token starts with: shpss_ (storefront) or similar
PUBLIC_STOREFRONT_API_TOKEN=your_storefront_token_here

# ─────────────────────────────────────────────────────────────
# REQUIRED: Session Security
# ─────────────────────────────────────────────────────────────

# Session secret - MUST be at least 32 characters
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=generate_a_secure_random_string_at_least_32_chars

# ─────────────────────────────────────────────────────────────
# OPTIONAL: Private API Access (Server-side only)
# ─────────────────────────────────────────────────────────────

# Admin API Token (for server-side operations like inventory, orders)
# Token starts with: shpat_
# PRIVATE_ADMIN_API_TOKEN=shpat_your_admin_token

# Private Storefront Token (for authenticated storefront operations)
# PRIVATE_STOREFRONT_API_TOKEN=your_private_storefront_token

# ─────────────────────────────────────────────────────────────
# OPTIONAL: Feature Flags
# ─────────────────────────────────────────────────────────────

# Set to "true" to show maintenance page instead of the app
MAINTENANCE_MODE=false

# ─────────────────────────────────────────────────────────────
# AUTO-PROVIDED: Shopify Oxygen (do not set manually)
# ─────────────────────────────────────────────────────────────
# These are automatically set when deploying to Oxygen
# PUBLIC_STOREFRONT_ID=
# SHOPIFY_STOREFRONT_ID=
# SHOPIFY_CHECKOUT_DOMAIN=
